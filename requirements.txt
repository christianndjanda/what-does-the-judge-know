# Kept deliberately small: every extra dependency is an aarch64 wheel risk on GH200.
# NOTE: do NOT pip-install torch on the Lambda image -- use the one that ships with it.
torch
transformers>=4.44
numpy
# accelerate is used only for device_map="auto" model loading.
accelerate
# apply_chat_template renders through Jinja; every prompt we build goes through it.
# Lambda Stack 22.04 ships 3.0.3, which is too old.
jinja2>=3.1
# Not used by this project. transformers eagerly imports its object-detection loss
# chain, which touches PIL.Image.Resampling (Pillow >= 9.1). Ubuntu 22.04's system
# Pillow is 9.0.1, so importing any model class fails without this.
Pillow>=9.1
